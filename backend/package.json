{
  "name": "edge-rate-limiter",
  "version": "1.0.0",
  "description": "Edge-inspired rate limiting and abuse detection service",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "db:migrate": "bun run src/db/migrate.ts",
    "db:seed": "bun run src/db/seed.ts"
  },
  "dependencies": {
    "@types/nanoid": "^3.0.0",
    "hono": "^4.11.4",
    "nanoid": "^5.0.9"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}